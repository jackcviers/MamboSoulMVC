@using System.Linq;

@model MamboSoulMVC.Models.ListViewModel


@{
    ViewBag.Title = "Home";
    Layout = "~/Views/_Layout.cshtml";
}

<br />
<div class="jumbotron">
    <i>
        Our love of dance has lead us to start the Mambo Soul Dance Company. If you are interested in becoming a more skilled latin dancer or just want to give it a try, please take a look around our website to learn about our mission, upcoming events, and learning opportunities. If you have any questions, please feel free to contact us.
        <br />
        <br />
        Thank you for visiting our website.<br />
        -Manuel de Aragon
    </i>
</div>
<br />

<div class="panel panel-default">
    <div class="panel-heading">Mambo Soul Dance Club <span style="color:#B80000; font-size:18px;">Mission Statement</span></div>
    <div class="panel-body" style="margin-left:20px;">Produce dancers equipped with the skills needed to perform and compete at the highest level desired</div>
    <div class="panel-heading">Mambo Soul Dance Club <span style="color:#B80000; font-size:18px;">Experience</span></div>
    <div class="panel-body" style="margin-left:20px;">Students receive latin dancing instruction from highly qualified and experienced teachers who have been teaching, performing and competing for many years. Students are given the opportunity to learn from instructors/performers/competitors/choreographers, who are ranked among the top in the world. Through comprehensive dancing instruction, consisting of technical skills (proper technique), fundamental movements and artistic expression, students gain the tools to reach their personal goals and maximum potential in dance</div>
</div>
<br />

<div id="myCarousel" class="carousel  slide" data-ride="carousel">
    <!-- Indicators -->
    <ol class="carousel-indicators">
        @if (Model.sliderImgs.Count > 0)
        {
            <li data-target="#myCarousel" data-slide-to="0" class="active"></li>

            for (int i = 1; i < (Model.sliderImgs.Count - 1); i++)
            {
                <li data-target="#myCarousel" data-slide-to="@i"></li>
            }
        }
    </ol>

    <div class="carousel-inner" role="listbox">
        @if (Model.sliderImgs.Count > 0)
        {
            var imageRoot = "~/Images/Slider/";
            var image = Model.sliderImgs.FirstOrDefault();
            var imagePath = imageRoot + image.FileName;
            var altText = image.Description ?? "img";

            <div class="item active">
                <img src="@Url.Content(imagePath)" alt="@altText" />
            </div>

            for (int i = 1; i < (Model.sliderImgs.Count - 1); i++)
            {
                image = Model.sliderImgs[i];

                imageRoot = "~/Images/Slider/";
                imagePath = imageRoot + image.FileName;
                altText = image.Description ?? "img";

                <div class="item">
                    <img src="@Url.Content(imagePath)" alt="@altText" />
                </div>
            }
        }
    </div>
    <!-- Left and right controls -->
    <a class="left carousel-control" href="#myCarousel" role="button" data-slide="prev">
        <span class="glyphicon glyphicon-chevron-left" aria-hidden="true"></span>
        <span class="sr-only">Previous</span>
    </a>
    <a class="right carousel-control" href="#myCarousel" role="button" data-slide="next">
        <span class="glyphicon glyphicon-chevron-right" aria-hidden="true"></span>
        <span class="sr-only">Next</span>
    </a>
</div>

<script>
    $(document).ready(function () {
        $("#navhome").removeClass("active").addClass("active");
        $("#navevents").removeClass("active");
        $("#navcontact").removeClass("active");
        $("#navabout").removeClass("active");
    });
</script>